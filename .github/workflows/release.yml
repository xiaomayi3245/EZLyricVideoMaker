name: Build & Release (Electron)

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install deps
        run: npm ci

      - name: Build app
        env:
          # Avoid build failures when signing certs are not configured.
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
        # Prevent electron-builder from trying to publish by itself (it would require GH_TOKEN).
        # We publish via softprops/action-gh-release using GitHub-provided GITHUB_TOKEN.
        run: npm run electron:build -- --publish never

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ runner.os }}-artifacts
          path: |
            release/*.exe
            release/*.dmg
            release/*.zip
          if-no-files-found: ignore

  github_release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/**/release/*.exe
            artifacts/**/release/*.dmg
            artifacts/**/release/*.zip

